import 'package:flutter/material.dart';

class EntrancePart extends StatelessWidget {
  const EntrancePart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
